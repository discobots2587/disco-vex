collectorSet(127);

				// wait 300 milleseconds to stabalize the robot after raising lift
drive(12, 127, 3000);				// drive forward to knock off beach ball closest to tile into goal zone
			// drive back to tile (wait for repositioning)
()waitforBump:
